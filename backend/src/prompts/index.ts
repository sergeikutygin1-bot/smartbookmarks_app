/**
 * Prompt Templates Index
 *
 * All prompts for the bookmark enrichment agent are centralized here.
 * Edit individual prompt files to customize the AI's behavior.
 */

export { analysisPrompt } from "./analysis";
export { taggingPrompt } from "./tagging";
export { judgePrompt } from "./judge";
